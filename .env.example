# Public origin of the site. Used for metadata and absolute URLs.
NEXT_PUBLIC_SITE_URL=https://mahmoud-hassan.ae

# Base URL of the CMS / backend.
# While this is unset, entities/*/api/* serve local seed content instead.
# API_BASE_URL=https://cms.example.com/api

# Where consultation requests from the contact form are forwarded.
# While this is unset, app/api/contact logs them and returns success.
# CONTACT_WEBHOOK_URL=
