/** Shared visual treatment for every form control, so they cannot drift apart. */
export const CONTROL_CLASS =
  "bg-dark-section border border-white/10 rounded-md p-3 w-full text-c-white text-sm " +
  "focus:outline-none focus:border-gold transition";

export const CONTROL_INVALID_CLASS = "border-red-500/70 focus:border-red-500";
